import type { ButtonOverrides } from 'baseui/button';

export const ICON_BUTTON_OVERRIDES: ButtonOverrides = {
  StartEnhancer: { style: () => ({ marginRight: '0' }) },
  EndEnhancer: { style: () => ({ marginLeft: '0' }) },
};
